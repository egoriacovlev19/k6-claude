import type { ProjectConfig } from '../../core/config';

// Все настройки Orange меняются здесь. В общее ядро для этого заходить не нужно.
const config: ProjectConfig = {
  environments: {
    production: 'https://my.orange.md/', // Начальный адрес. Сайт сам перенаправляет на SSO.
  },

  timeoutMs: 60000, // Максимальное время одного теста: 60 секунд.

  // Это стартовые допуски для проверки, а не согласованные требования Orange.
  budgets: {
    pageReadyMs: 30000,   // За сколько должна появиться вся проверяемая форма.
    elementReadyMs: 3000, // Конкретный элемент ждём максимум 3 секунды.
  },

  profiles: {
    smoke: {
      ui: {
        workers: 10,            // Сколько тестов может идти одновременно (аккаунтов сейчас меньше).
        durationSeconds: 0,     // 0 = выполнить repeats раз. Например, 60 = повторять партии 60 секунд.
        repeats: 1,             // Один вход на каждый тестовый аккаунт.
        lineIntervalSeconds: 5, // Следующий аккаунт начинает тест через 5 секунд после предыдущего.
      },
    },
  },

  // Тестовые аккаунты (логины и пароли) — не здесь: они в data/pages.csv,
  // путь к файлу передаётся флагом --data.
  //
  // Настройки нагрузки (rps, длительность, пороги) — тоже не здесь, а в
  // projects/orange/load/scenario.js. Ядро про них ничего не знает.
};

export default config;
